<form method="post" action="{{ path('app_sub_category_delete', {'id': sub_category.id}) }}" onsubmit="return confirm('Вы уверены что хотите удалить эту подкатегорию?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_category.id) }}">
    <br>
    <button class="btn btn-danger">Удалить</button>
</form>
