{% extends 'base.html.twig' %}

{% block title %}Изменить подкатегорию{% endblock %}

{% block body %}
    <div class="container container-body">
        <br>
        <h1>Изменить подкатегорию</h1>
        <br>
        {{ include('sub_category/_form.html.twig', {'button_label': 'Сохранить'}) }}
        <br>
        <a class="btn btn-outline-primary" href="{{ path('app_sub_category_index') }}">Вернуться к списку</a>

        {{ include('sub_category/_delete_form.html.twig') }}
    </div>
{% endblock %}
